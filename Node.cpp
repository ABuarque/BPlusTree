#include "Node.h"
#include <vector>
#include <string>
#include "utils.h"

using namespace std;

/*
Node::Node() {}

Node::~Node() {}

int Node::keysSize() {
	return keys.size();
}

string Node::toString() {
	string keysState = "[ ";
	for(int i = 0; i < keys.size(); i++) { 
		keysState += intToString(keys[i]);
		if((i + 1) < keys.size())
			keysState += ", ";
	}
	keysState += " ]";
	return keysState;
}
*/
